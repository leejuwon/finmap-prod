# Hreflang Opt-Out Implementation - 2026-06-26

## Summary

- Target slug: `how-much-per-month-for-100m`
- Decision source: `reports/how-much-per-month-100m-hreflang-audit.md`
- Final behavior: URL, canonical, robots, noindex, and slug remain unchanged. Only hreflang equivalent signals are decoupled.
- Scope guard: no body content changes were made to KO/EN posts.

## Files Changed

| File | Change |
| --- | --- |
| `content/posts/personalFinance/ko/how-much-per-month-for-100m.md` | Added `hreflangEquivalent: false` in frontmatter. |
| `content/posts/personalFinance/en/how-much-per-month-for-100m.md` | Added `hreflangEquivalent: false` in frontmatter. |
| `lib/posts.js` | Exposes `hreflangEquivalent` on the post object. |
| `pages/posts/[category]/[slug].js` | Passes `alternateLanguages={post.hreflangEquivalent !== false}` to `SeoHead`. |
| `next-sitemap.config.js` | Reads frontmatter opt-out and emits self-only sitemap hreflang refs for opt-out posts. |
| `scripts/verify_post_publish_urls.js` | Reads frontmatter opt-out and accepts self-only hreflang as PASS. |
| `scripts/verify_seo_channel_split.js` | Reads frontmatter opt-out, adds target samples, and keeps normal pair regression samples. |
| `public/sitemap-0.xml` | Regenerated; target KO/EN entries now have self-only `xhtml:link`. |
| `public/sitemap-ko.xml` | Regenerated; target KO entry now has self-only `xhtml:link`. |
| `public/sitemap-en.xml` | Regenerated; target EN entry now has self-only `xhtml:link`. |
| `public/en/sitemap.xml` | Regenerated; target EN entry now has self-only `xhtml:link`. |
| `reports/seo-channel-split-url-check.md` | Regenerated by `verify_seo_channel_split.js` with opt-out and normal pair samples. |
| `reports/hreflang-opt-out-implementation-2026-06-26.md` | This implementation report. |

## Frontmatter Opt-Out

Both target files now include:

```yaml
hreflangEquivalent: false
```

This is frontmatter-only. The article body, title, slug, canonical URL, robots behavior, and noindex behavior were not changed.

## Head Hreflang Behavior

`SeoHead` already supported `alternateLanguages={false}` as self-only hreflang output. The post page now passes that value from frontmatter.

Target local verification:

| URL | Canonical | Hreflang mode | Result |
| --- | --- | --- | --- |
| `https://www.finmaphub.com/posts/personalFinance/how-much-per-month-for-100m` | self | `ko` self-only, no `en` alternate | PASS |
| `https://www.finmaphub.com/en/posts/personalFinance/how-much-per-month-for-100m` | self | `en` self-only, no `ko` alternate | PASS |

Normal pair regression:

| URL | Hreflang mode | Result |
| --- | --- | --- |
| `https://www.finmaphub.com/posts/personalFinance/what-is-cagr` | `ko` + `en` pair | PASS |
| `https://www.finmaphub.com/en/posts/personalFinance/what-is-cagr` | `ko` + `en` pair | PASS |

## Sitemap Hreflang Behavior

Target entries after `npm.cmd run build` / `postbuild`:

| Sitemap | Target entry behavior |
| --- | --- |
| `public/sitemap-0.xml` | KO loc remains; only `hreflang="ko"` self link. EN loc remains; only `hreflang="en"` self link. |
| `public/sitemap-ko.xml` | KO loc remains; only `hreflang="ko"` self link. |
| `public/sitemap-en.xml` | EN loc remains; only `hreflang="en"` self link. |
| `public/en/sitemap.xml` | EN loc remains; only `hreflang="en"` self link. |

Confirmed with:

```text
rg -n "how-much-per-month-for-100m" public\sitemap-0.xml public\sitemap-ko.xml public\sitemap-en.xml public\en\sitemap.xml
```

Normal pair regression was also checked with `what-is-cagr`; both KO and EN sitemap entries still contain `hreflang="ko"` and `hreflang="en"`.

## Verifier Changes

- `verify_post_publish_urls.js`
  - Builds an opt-out URL set from post frontmatter.
  - For opt-out URLs, expects self-only hreflang and fails if the opposite language hreflang is present.
  - Continues to check HTTP 200, final URL, self canonical, robots, noindex, sitemap membership, and RSS for KO.
- `verify_seo_channel_split.js`
  - Builds an opt-out path set from post frontmatter.
  - Adds the target KO/EN URLs as `post-opt-out` samples.
  - Adds `what-is-cagr` KO/EN as normal `post-pair` regression samples.
  - For opt-out samples, expects self-only hreflang while preserving canonical and sitemap membership checks.

## Validation Results

| Command | Result |
| --- | --- |
| `node --check next-sitemap.config.js` | PASS |
| `node --check scripts\verify_post_publish_urls.js` | PASS |
| `node --check scripts\verify_seo_channel_split.js` | PASS |
| `node --check scripts\generate_channel_sitemaps.js` | PASS |
| `npm.cmd run build` | PASS. Next build completed and postbuild regenerated sitemap files. |
| `node scripts\verify_post_publish_urls.js --local-server <target KO/EN URLs>` | PASS. Both target URLs returned 200, self canonical, no robots block, no noindex, sitemap included, self-only hreflang. |
| `node scripts\verify_seo_channel_split.js --local-server` | PASS. 20 samples, 0 failures. Target opt-out and `what-is-cagr` normal pair samples passed. |
| `node scripts\verify_post_publish_urls.js --local-server <what-is-cagr KO/EN URLs>` | PASS. Both normal pair URLs kept `ko` + `en` hreflang pair. |
| `rg -n "how-much-per-month-for-100m" public\sitemap-0.xml public\sitemap-ko.xml public\sitemap-en.xml public\en\sitemap.xml` | PASS. Target locs remain; opposite language alternates removed. |
| `rg -n "what-is-cagr" public\sitemap-0.xml public\sitemap-ko.xml public\sitemap-en.xml public\en\sitemap.xml` | PASS. Normal pair keeps both alternates. |
| `git diff --check` | PASS. Exit code 0; LF/CRLF conversion warnings only. |

## Remaining Notes

- `npm.cmd run build` regenerated sitemap files from current content. Besides the target hreflang opt-out, sitemap diffs include current content lastmod/order/loc updates that were already implied by the repository's present content state.
- `scripts/generate_channel_sitemaps.js` did not need custom opt-out code because it preserves the source `sitemap-0.xml` entry XML. The opt-out is applied at the `next-sitemap.config.js` source generation stage.
- The other P0 decouple candidate for the DCA bear market slug was not touched.
